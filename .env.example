# Upstash Redis REST API (console.upstash.com → REST API)
# These are embedded in the Vite client bundle — use only for personal/prototipo setups.
VITE_UPSTASH_REDIS_REST_URL=
VITE_UPSTASH_REDIS_REST_TOKEN=

# Optional namespace for multi-tenant keys (default: default)
# VITE_TENANT_ID=default
